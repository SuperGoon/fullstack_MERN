import React from "react";

import makeStyles from "./styles";

function Form(props) {
    const classes = makeStyles();
    return (
        <div>
            <h1>FORM</h1>
        </div>
    );
}

export default Form;
